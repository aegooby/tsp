
#include	"__common.hpp"
#include	"graph.hpp"
#include	"tsp_brute.hpp"
#include	<list>

int	main(int argc, char** argv)
{
//	tsp::graph<float, size_t>	graph(44);
//	graph.add_vertex(0);
//	graph.add_vertex(1);
//	graph.add_edge(0, 1, 5);
//	for (const auto& i : graph.edges())
//		std::cout << i << std::endl;
	
	return 0;
}
